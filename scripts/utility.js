function seatSelecteda2() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-a2');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-a2');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'A2';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}

// A2 end 

// A3 start 
function seatSelecteda3() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-a3');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-a3');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'A3';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}
// A3 end 

// A4 start 
function seatSelecteda4() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-a4');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-a4');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'A4';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}
// A4 end 

// B1 start 
function seatSelectedb1() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-b1');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-b1');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'B1';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}
// B1 end 

// B2 start
function seatSelectedb2() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-b2');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-b2');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'B2';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}

// B2 end 

// B3 start
function seatSelectedb3() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-b3');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-b3');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'B3';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}

// B3 end 

// B4 start 
function seatSelectedb4() {
    /**ticket selecting functionality start */
    const availableSeat = document.getElementById('seat-left');
    const newSeat = Math.floor(availableSeat.innerText - 1);
    availableSeat.innerText = newSeat;
    
    const seatsLeft = document.getElementById('seat-left-now-b4');
    seatsLeft.classList.remove('bg-[#F7F8F8]');
    
    const seatLeft = document.getElementById('seat-left-now-b4');
    seatLeft.classList.add('bg-[#1DD100]', 'text-white');

    const seatSelected = document.getElementById('seat-selected');
    seatSelected.classList.add('text-[#1DD100]', 'font-medium');
    /**ticket selecting functionalities end */

    /**ticket pricing calculation section start */
    const seatNum = document.getElementById('seat-num');
    seatNum.innerText = 'B4';

    const ticketPrice = document.getElementById('ticket-price');
    ticketPrice.innerText = 550;

    const totalPrice = document.getElementById('total-price');
    totalPrice.innerText = 550;

    const enableCoupon = document.getElementById('coupon');
    enableCoupon.removeAttribute('disabled');
    const enableCpnBtn = document.getElementById('coupon-btn');
    enableCpnBtn.removeAttribute('disabled');

    const nextbtn = document.getElementById('next-btn');
    nextbtn.classList.remove('pointer-events-none');

    window.alert('Site under Construction: you can buy only one seat. Without this issue everything is okay');
}

// B4 end 